services:
  - type: web
    name: veloza-motors
    env: node
    region: oregon
    buildCommand: npm install && cd frontend && npm install && npm run build
    startCommand: node src/app.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: DB_URI
        value: mongodb+srv://adminGSD:Vesper24@velozamotors.n1pxmh4.mongodb.net/veloza_motors?retryWrites=true&w=majority
